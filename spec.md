# Specification

## Summary
**Goal:** Update the contact email address to `sv.couverture95@gmail.com` across the entire site.

**Planned changes:**
- Replace the existing email with `sv.couverture95@gmail.com` in the Contact section info card
- Replace the existing email with `sv.couverture95@gmail.com` in the Footer contact details
- Update the email field in the JSON-LD LocalBusiness structured data in `index.html` if present
- Ensure every displayed email is a clickable `mailto:sv.couverture95@gmail.com` link

**User-visible outcome:** The new email address appears consistently and is clickable in all locations where contact information is shown on the site.
