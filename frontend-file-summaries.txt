{
  "frontend/index.html": "SEO-optimized HTML entry point with all existing meta/OG/Twitter/JSON-LD tags preserved, plus performance improvements (preload, async fonts, inlined critical CSS). Fixed the scroll bug by scoping the sticky/fixed positioning rule from the broad `nav, header` selector to `header[role=\"banner\"]` only, so only the navigation banner is fixed and all other `<header>` elements inside page sections scroll normally.",
  "frontend/src/index.css": "Global stylesheet with OKLCH warm earthy color system (terracotta, charcoal, offwhite), Playfair Display serif headings, Inter sans-serif body, reusable component classes (btn-terracotta, card-service, testimonial-card, form-input, tile-pattern), and smooth scroll behavior.",
  "frontend/src/App.tsx": "Root application component that composes all landing page sections (Navigation, Hero, Services, About, Testimonials, Contact, Footer) in a single-page layout.",
  "frontend/src/components/Navigation.tsx": "Sticky responsive navigation bar for VERDIER COUVERTURE with scroll-aware background, anchor links, dual phone CTAs (mobile 06 63 73 94 00 and landline 01 72 76 17 63) with tel: links, and mobile hamburger menu — all in French.",
  "frontend/src/components/Hero.tsx": "Full-screen hero section for VERDIER COUVERTURE with background image, H1 heading, highlights list, both phone numbers (mobile and landline) as clickable tel: links, dual CTA buttons, and a stats bar — RGE badge replaced with \"Couvreur Professionnel Certifié\".",
  "frontend/src/components/Services.tsx": "Six-card services grid with all six service card `<img>` elements now having `loading=\"lazy\"` and `decoding=\"async\"` to prevent below-the-fold images from downloading during initial page load, while keeping all visual styles and content unchanged.",
  "frontend/src/components/About.tsx": "Two-column about section with the quality icon `<img>` now having `loading=\"lazy\"` and `decoding=\"async\"` to defer its load until the user scrolls near the section, with all other content and styles unchanged.",
  "frontend/src/components/Testimonials.tsx": "Six testimonial cards with Schema.org Review microdata, now including the required `itemReviewed` property (LocalBusiness with name \"VERDIER COUVERTURE\") on each review article to fix Google's \"itemReviewed missing\" error, plus properly nested `reviewRating`, `author` (Person), and `datePublished` microdata attributes.",
  "frontend/src/components/Contact.tsx": "Contact section with devis form that now correctly opens a mailto: link to sv.couverture95@gmail.com with all form data pre-filled in the subject and body, plus an info banner explaining the email destination, and an updated confirmation screen showing the recipient address.",
  "frontend/src/components/Footer.tsx": "Four-column footer for VERDIER COUVERTURE with phone numbers, updated email (sv.couverture95@gmail.com) as a clickable mailto: link, address, services/navigation links, zone d'intervention, copyright, and caffeine.ai attribution.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Playfair Display serif and Inter sans-serif font families, brand color tokens (terracotta, slate, offwhite, charcoal, cream), and custom box-shadow utilities.",
  "frontend/public/google2ea9c3e7d5d4abde.html": "Static HTML file for Google Search Console ownership verification, containing the google-site-verification meta tag with token \"google2ea9c3e7d5d4abde\" and the required body text.",
  "frontend/public/sitemap.xml": "Valid XML sitemap with a single `<url>` entry for the canonical root URL `https://site-de-couverture-seo-3wk.caffeine.xyz/`, using `lastmod` date `2025-07-17`, no trailing newline, and no fragment/anchor URLs.",
  "frontend/public/robots.txt": "Plain-text robots.txt allowing all crawlers with `User-agent: *` / `Allow: /` and the absolute `Sitemap:` directive on the next line with no blank lines or trailing whitespace.",
  "No files were modified — all existing files are already correctly configured for the production domain https": "//site-de-couverture-seo-3wk.caffeine.xyz/ and no changes are required."
}